using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//Referenced from user Komizart in unity forums https://forum.unity.com/threads/screen-shake-effect.22886/ 
/*public class Shaker : MonoBehaviour {

    private float ShakeY = 0.8f;
    private float ShakeYSpeed = 0.8f;


    public void SetShake(float someY)
    {
        ShakeY = someY;
    }

    void Update()
    {
        Vector2 _newPosition = new Vector2(0, ShakeY);
        if (ShakeY < 0)
        {
            ShakeY *= ShakeYSpeed;
        }
        ShakeY = -ShakeY;
        transform.Translate(_newPosition, Space.Self);
    }
}*/
